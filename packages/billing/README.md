# @truesuite/billing

Shared Stripe helpers: checkout session creation, customer portal, webhook signature verification.

Empty for now. Initial implementation will extract patterns from `apps/trueqr/app/api/checkout/` and `apps/trueqr/app/api/stripe-webhook/`.
