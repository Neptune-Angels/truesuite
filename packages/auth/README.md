# @truesuite/auth

Shared Supabase auth client + Next.js middleware helpers.

Empty for now. Initial implementation will move `apps/trueqr/lib/supabase.ts`, `lib/supabase-admin.ts`, and `middleware.ts` patterns into here once we have a second app actually consuming auth.

Consumed via `"@truesuite/auth": "workspace:*"`.
